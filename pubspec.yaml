name: my_wallet
description: "Assignment Project for Internship opportunity @Persistent_Ventures"
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  fluttertoast: ^8.2.6
  provider: ^6.1.2
  dartz: ^0.10.1
  http: ^1.2.1
  get_it: ^7.7.0
  shared_preferences: ^2.2.3
  gradient_borders: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  fonts:
    - family: Playwrite
      fonts:
        - asset: assets/custom_font.ttf

